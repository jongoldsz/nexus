<%= form_for(@blogentry) do |f| %>
  <% if @blogentry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blogentry.errors.count, "error") %> prohibited this blogentry from being saved:</h2>

      <ul>
      <% @blogentry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :highlighted %><br />
    <%= f.check_box :highlighted %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
