<h1>Listing blogentries</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Content</th>
    <th>Highlighted</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @blogentries.each do |blogentry| %>
  <tr>
    <td><%= blogentry.title %></td>
    <td><%= blogentry.content %></td>
    <td><%= blogentry.highlighted %></td>
    <td><%= link_to 'Show', blogentry %></td>
    <td><%= link_to 'Edit', edit_blogentry_path(blogentry) %></td>
    <td><%= link_to 'Destroy', blogentry, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Blogentry', new_blogentry_path %>
